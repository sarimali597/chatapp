# Port the server listens on.
# Render sets this automatically in production — only needed for local dev.
PORT=3000

# Where your deployed React frontend lives, for CORS.
# Comma-separate multiple origins if needed, e.g.:
# CLIENT_URL=https://chatapp-frontend.onrender.com,http://localhost:5173
CLIENT_URL=http://localhost:5173
